global:
  scrape_interval: 15s

rule_files:
  - "alertmanager-alerts.yml"

scrape_configs:
  - job_name: 'skolab-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:8000']
