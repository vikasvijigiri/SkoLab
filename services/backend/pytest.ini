[pytest]
# Many async tests (test_backend_reliability, test_data_quality, test_discovery,
# ...) have no @pytest.mark.asyncio marker and rely on auto mode. Without this
# they error under pytest-asyncio's default strict mode. There is no other
# pytest config file in the repo, so this is the one place it can live.
asyncio_mode = auto
testpaths = tests
