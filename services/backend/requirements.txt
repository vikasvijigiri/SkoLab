fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.12.5
python-dotenv==1.2.3
httpx==0.28.1
firebase-admin==7.5.0
numpy==2.2.6
# Embeddings run via the Hugging Face Inference API (see
# app/services/ai/embedding_service.py). `sentence-transformers` (and its
# ~2 GB PyTorch dependency) is intentionally NOT listed — add it only for a
# self-hosted deploy that wants the local model backend.
networkx==3.4.2
zeroconf==0.151.3
pdfplumber==0.11.10
python-multipart==0.0.32
tenacity==9.1.4
# Database
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
openrouter==1.1.108
defusedxml==0.7.1
psutil==7.2.2
redis==8.1.0
aiosqlite==0.22.1
# Observability
sentry-sdk[fastapi]==2.42.0
# sentry-sdk's StarletteIntegration unconditionally imports jinja2 at
# sentry_sdk.init() time (to patch Jinja2Templates), even though this API
# never renders HTML templates. Without it installed, init_observability()
# crashes the whole process on boot as soon as SENTRY_DSN is set.
jinja2==3.1.5
