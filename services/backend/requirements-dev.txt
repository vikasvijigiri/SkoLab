# Test + lint tooling for the backend. Install with:
#   pip install -r requirements-dev.txt
# Runtime deps come transitively via the line below.
-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.4.0
ruff==0.16.3
mypy==2.3.1
bandit==1.9.4
pip-audit==2.10.1
